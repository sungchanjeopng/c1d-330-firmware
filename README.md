# C1D-330 Firmware

STM32F769 C1D-330 firmware source snapshot prepared for GitHub.

Local source path used for this snapshot:
`D:\tom\ble\ble\ble0507\ble0507\c1d-330 v1_1_0`

Excluded from this repository:
- STM32CubeIDE build outputs (`Debug/`, `Release/`, `*.elf`, `*.bin`, etc.)
- agent working folders (`.hermes/`, `.omx/`, `.omc/`)
- local temporary/backup files

Build artifacts should be generated locally from STM32CubeIDE / the generated `Debug` makefile.
